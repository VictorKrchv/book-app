let initialState = {
    genres: ["Romance", "Action & Adventure", "Mystery & Thriller", "Children's", "Historical Fiction", "Fantasy"]
}


const genresReducer = (state = initialState, action) => {
    return state;
}

export default genresReducer;